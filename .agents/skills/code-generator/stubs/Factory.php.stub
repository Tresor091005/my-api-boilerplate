<?php

declare(strict_types=1);

namespace Lahatre\{{Module}}\Database\Factories;

use Illuminate\Database\Eloquent\Factories\Factory;
use Lahatre\{{Module}}\Models\{{Entity}};

/**
 * @extends \Illuminate\Database\Eloquent\Factories\Factory<\{{Module}}\Models\{{Entity}}>
 */
class {{ClassName}} extends Factory
{
    use ResolvesOrganizationId;

    public function definition(): array
    {
        return [
            // Add only fields owned by this model.
            // Tenant-owned factories should use ResolvesOrganizationId and
            // assign organization_id explicitly.
        ];
    }
}
