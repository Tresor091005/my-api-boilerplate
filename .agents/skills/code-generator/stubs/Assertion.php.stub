<?php

declare(strict_types=1);

namespace Lahatre\{{Module}}\Assertions;

use Illuminate\Support\Collection;
use Lahatre\{{Module}}\Exceptions\{{Entity}}Exception;
use Lahatre\{{Module}}\Models\{{Entity}};

final class {{Entity}}Assertion
{
    /**
     * Assert that the supplied related models can participate in the requested
     * operation without creating an invalid {{Entity}} state.
     *
     * The service supplies this Collection because it also needs the models for
     * persistence. Do not query the same evidence again inside this Assertion.
     *
     * @param  Collection<string, object>  $relatedModelsById
     *
     * @throws {{Entity}}Exception If a supplied model is not authentic or cannot participate in the operation.
     */
    public function assertCanPerformAction({{Entity}} $entity, Collection $relatedModelsById): void
    {
        // Compare the supplied evidence and throw a named exception factory on failure.
    }
}
