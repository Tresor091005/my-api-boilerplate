<?php

declare(strict_types=1);

use Illuminate\Foundation\Testing\RefreshDatabase;

uses(RefreshDatabase::class);

/*
 * Choose the correct test boundary before filling this template:
 *
 * - HTTP behavior belongs in a Feature test through the route.
 * - Isolated Assertions, Data objects, and service contracts belong in Unit tests.
 * - Cross-module authorization and tenancy belong in tests/Feature/Integration.
 *
 * Replace this comment with behavior-focused tests and real factories. Never
 * leave an empty generated test in the suite.
 */
