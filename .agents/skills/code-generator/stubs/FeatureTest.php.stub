<?php

declare(strict_types=1);

namespace Lahatre\{{Module}}\Tests\Feature;

use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Validation\ValidationException;
use Lahatre\{{Module}}\DTO\{{Entity}}DTO;
use Lahatre\{{Module}}\Models\{{Entity}};
use Lahatre\{{Module}}\Services\{{Entity}}Service;
use Tests\TestCase;

uses(TestCase::class, RefreshDatabase::class);

it('can perform successful action', function (): void {
    // 1. Arrange
    // ${{EntityLower}} = {{Entity}}::factory()->create();

    // 2. Act
    // $dto = new {{Entity}}DTO([...]);
    // $result = app({{Entity}}Service::class)->create($dto);

    // 3. Assert
    // expect($result)->toBeInstanceOf({{Entity}}Resource::class);
});

it('fails validation with invalid data', function (): void {
    // $dto = new {{Entity}}DTO(['invalid' => 'data']);
})->throws(ValidationException::class);

it('fails when business rule is violated', function (): void {
    // provocate AssertionException
})->throws(\Lahatre\Shared\Exceptions\AssertionException::class);
