<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Route;
use Lahatre\{{Module}}\Http\Controllers\{{Entity}}Controller;

/* -----------------------------------------------------------------
 | {{ModuleName}} endpoints
 | -----------------------------------------------------------------
 */
Route::group([
    'as'         => 'lahatre.{{ModuleLower}}.',
    'prefix'     => 'v1/{{ModuleLower}}',
    'middleware' => 'api',
], function (): void {
    Route::group([
        'middleware' => 'auth.api',
    ], function (): void {
        Route::apiResource('{{resourcePlural}}', {{Entity}}Controller::class);
    });
});
