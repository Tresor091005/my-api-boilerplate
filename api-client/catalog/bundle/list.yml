info:
  name: list with components
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/catalog/bundles?include=items"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
