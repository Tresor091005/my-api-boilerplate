info:
  name: switch-member-role
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/v1/auth/switch-member-role"
  body:
    type: json
    data: |-
      {
        "member_role_id": "019d3997-0a1a-72ae-a252-ff5bfcee64a7"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
