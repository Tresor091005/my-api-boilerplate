info:
  name: forgot-password
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/v1/auth/forgot-password"
  body:
    type: json
    data: |-
      {
        "email": "admin@lahatre.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
