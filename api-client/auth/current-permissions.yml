info:
  name: current-permissions
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/v1/auth/current-permissions"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
