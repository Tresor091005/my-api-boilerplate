info:
  name: logout
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/v1/auth/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
